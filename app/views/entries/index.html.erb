<% content_for :title do %>
  Foodlog &ndash; <%= DateTime.now.to_formatted_s(:nice_date) %>
<% end %>

<div class="columns mt-6 is-centered" id="entries">
  <% @entries.each do |entry| %>
    <%= render entry %>
  <% end %>
</div>

<%# <%= link_to "New entry", new_entry_path %>
